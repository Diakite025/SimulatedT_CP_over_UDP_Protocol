#include "mecanisme.h"


/**void ajout_data_to_segment(segment_tcp * segment, char* message ){

    // size of message <=44
    segment->data=malloc(strlen(message));
    strcpy(segment->data,message);

}
**/